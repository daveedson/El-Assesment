import 'package:flutter/material.dart';

class ShipementsModel {
  final String progressText;
  final Color textColor;
  final String price;
  final Widget progressIcon;
  ShipementsModel({required this.progressText, required this.textColor, required this.price, required this.progressIcon});
}
